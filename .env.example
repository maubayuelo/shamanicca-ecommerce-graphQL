# Copy this file to .env.local for local development and replace values with real secrets.
# Never commit real secrets to the repository.

# Public-facing variables (prefix with NEXT_PUBLIC_ to expose to the client)
NEXT_PUBLIC_GRAPHQL_URL=https://api.example.com/graphql
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_GA_ENABLED=false

# Server-side secrets (do NOT expose to the client)
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXX

# SMTP (for nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASSWORD=supersecret

# Optional: third-party service API keys
SENDGRID_API_KEY=
SENTRY_DSN=

# Local dev convenience
NODE_ENV=development
PORT=3000

# Notes:
# - Copy this file to .env.local (gitignored) and fill in the real values.
# - For production, set these values in your hosting provider's environment settings.
