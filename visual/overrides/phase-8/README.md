This synthetic announcement response is used only by the three
`home__*__announcement-visible` captures. The recorded site-settings response
is empty, and record-missing is additive, so this override is intentionally
kept outside the main fixture store.
